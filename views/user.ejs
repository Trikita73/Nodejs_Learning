<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express</title>
</head>
<body>
    <h1>Page user: <%= username %></h1>
    <% if(username.toLowerCase() == 'andrii') { %>
    <ul>
        <% hobbies.forEach((el) => { %>
            <li><%= el %></li>
        <% }) %>
    </ul>
    <% } %>
</body>
</html>